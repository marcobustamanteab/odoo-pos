<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">

   <t t-name="ClientDetailsEdit" t-inherit="point_of_sale.ClientDetailsEdit" t-inherit-mode="extension" owl="1">
        <xpath expr="//div[hasclass('client-details-left')]" position="replace">
            <div class="client-details-left">
                <div class="client-detail">
                    <t t-if="props.partner.is_company">
                        <span class="label">Persona</span>
                        <input type="radio" name="is_company" value="false" t-on-change="captureChange"
                               t-on-click="trigger('person-type-validate')"/>
                    </t>
                    <t t-else="!props.partner.is_company">rpc
                        <span class="label">Persona</span>
                        <input type="radio" name="is_company" value="true" checked="true" t-on-change="captureChange"/>
                    </t>
                </div>
                <div class="client-detail">
                    <span class="label">Calle *</span>
                    <input class="detail client-address-street" name="street"
                           t-on-change="captureChange" t-att-value="props.partner.street || ''"
                           placeholder="Street" />
                </div>
                <div class="client-detail">
                    <span class="label">Ciudad *</span>
                    <input class="detail client-address-city" name="city"
                           t-on-change="captureChange" t-att-value="props.partner.city || ''"
                           placeholder="City" />
                </div>
<!--                <div class="client-detail">-->
<!--                    <span class="label">Postcode</span>-->
<!--                    <input class="detail client-address-zip" name="zip"-->
<!--                           t-on-change="captureChange" t-att-value="props.partner.zip || ''"-->
<!--                           placeholder="ZIP" />-->
<!--                </div>-->
                <div class="client-detail">
                    <span class="label">Comuna *</span>
                    <select class="detail client-address-states needsclick" name="state_id"
                            t-on-change="captureChange">
                        <option value="">None</option>
<!--                        <option value=""><t t-esc="customerTrx"/></option>-->
<!--                            chile_id = 46-->
<!--                        <t t-if="props.partner.is_company">-->
                            <t t-foreach="env.pos.states" t-as="state" t-key="state.id">
                                <option t-if="props.partner.country_id[0] == state.country_id[0]"
                                        t-att-value="state.id"
                                        t-att-selected="props.partner.state_id ? ((state.id === props.partner.state_id[0]) ? true : undefined) : undefined">
                                    <t t-esc="state.name" />
                                </option>
                            </t>
<!--                        </t>-->
                    </select>
                </div>
                <div class="client-detail">
                    <span class="label">Pais *</span>
                    <select class="detail client-address-country needsclick" name="country_id"
                            t-on-change="captureChange">
<!--                    <select class="detail client-address-country needsclick" name="country_id"-->
<!--                            t-on-change="(props.partner.country_id[0] == env.state.country_id[0]) ? captureChange : refreshStatesPos">-->
                        <option value="">None</option>
                        <t t-foreach="env.pos.countries" t-as="country" t-key="country.id">
                            <option t-att-value="country.id"
                                    t-att-selected="props.partner.country_id ? ((country.id === props.partner.country_id[0]) ? true : undefined) : undefined">
                                <t t-esc="country.name" />
                            </option>
                        </t>
                    </select>
                </div>
               <div class="client-detail" t-if="props.partner.is_company">
                    <span class="label">Tipo de Contribuyente
                        <t t-if="props.partner.is_company">
                            *
                        </t>
                    </span>
                    <select class="detail client-address-states needsclick" name="l10n_cl_sii_taxpayer_type"
                            t-on-change="captureChange">
                      <option value="">Tipo de Contribuyente</option>
                      <option value="1">IVA afecto 1ª categoría</option>
                      <option value="2">Emisor de boleta 2da categoría</option>
                      <option value="3">Consumidor final</option>
                      <option value="4">Extranjero</option>
                    </select>
                </div>
                <div class="client-detail" t-if="props.partner.is_company">
                    <span class="label">Giro
                        <t t-if="props.partner.is_company">
                            *
                        </t>
                    </span>
                    <input class="detail client-address-street" name="l10n_cl_activity_description"
                                                   t-on-change="captureChange" t-att-value="props.partner.l10n_cl_activity_description || ''"
                                                   placeholder="Giro" />
                </div>
            </div>
        </xpath>
    </t>

   <t t-name="ClientDetailsEdit" t-inherit="point_of_sale.ClientDetailsEdit" t-inherit-mode="extension" owl="1">
        <xpath expr="//div[hasclass('client-details-right')]" position="replace">
            <div class="client-details-right">
                <div class="client-detail">
                    <t t-if="props.partner.is_company">
                        <span class="label">Empresa</span>
                        <input type="radio" name="is_company" checked="true" t-on-change="captureChange"/>
                    </t>
                    <t t-else="!props.partner.is_company">
                        <span class="label">Empresa</span>
                        <input type="radio" name="is_company" value="false" t-on-change="captureChange"
                               t-on-click="trigger('company-type-validate')"/>
                    </t>
                </div>
<!--                <div class="client-detail">-->
<!--                    <span class="label">Language</span>-->
<!--                    <select class="detail client-lang needsclick" name="lang"-->
<!--                            t-on-change="captureChange">-->
<!--                        <t t-foreach="env.pos.langs" t-as="lang" t-key="lang.id">-->
<!--                            <option t-att-value="lang.code"-->
<!--                                    t-att-selected="props.partner.lang ? ((lang.code === props.partner.lang) ? true : undefined) : lang.code === env.pos.user.lang? true : undefined">-->
<!--                                <t t-esc="lang.name" />-->
<!--                            </option>-->
<!--                        </t>-->
<!--                    </select>-->
<!--                </div>-->
                <div class="client-detail">
                    <span class="label">Email *</span>
                    <input class="detail client-email" name="email" type="email"
                           t-on-change="captureChange"
                           t-att-value="props.partner.email || ''" />
                </div>
                <div class="client-detail">
                    <span class="label">Celular *</span>
                    <input class="detail client-phone" name="phone" type="tel"
                           t-on-change="captureChange"
                           t-att-value="props.partner.mobile || ''" />
                </div>
<!--                <div class="client-detail">-->
<!--                    <span class="label">Barcode</span>-->
<!--                    <input class="detail barcode" name="barcode" t-on-change="captureChange"-->
<!--                           t-att-value="props.partner.barcode || ''" />-->
<!--                </div>-->
                <div class="client-detail">
                    <span class="label">RUT
                        <t t-if="props.partner.is_company">
                            *
                        </t>
                    </span>
                    <input class="detail vat" name="vat" t-on-change="captureChange"
                           t-att-value="props.partner.vat || ''" />
                </div>
<!--                <div t-if="env.pos.pricelists.length gt 1" class="client-detail">
                    <span class="label">Pricelist</span>
                    <select class="detail needsclick" name="property_product_pricelist"
                            t-on-change="captureChange">
                        <t t-foreach="env.pos.pricelists" t-as="pricelist"
                           t-key="pricelist.id">
                            <option t-att-value="pricelist.id"
                                    t-att-selected="props.partner.property_product_pricelist ? (pricelist.id === props.partner.property_product_pricelist[0] ? true : false) : false">
                                <t t-esc="pricelist.display_name" />
                            </option>
                        </t>
                    </select>
                </div>-->
<!--                <div class="client-detail">
                    <span class="label">Edad</span>
                    <input class="detail client-address-street" name="age"
                                   t-on-change="captureChange" t-att-value="props.partner.age || ''"
                                   placeholder="Edad" />
                </div>-->
<!--                <div class="client-detail">-->
<!--                    <span class="label">Categoría</span>-->
<!--                    <input class="detail client-address-street" name="category"-->
<!--                                   t-on-change="captureChange" t-att-value="props.partner.category || ''"-->
<!--                                   placeholder="Categoria" />-->
<!--                </div>-->
                <div class="client-detail">
                    <span class="label">Fecha de Nacimiento</span>
                    <input type="date" name="dob" t-on-change="captureChange" t-att-value="props.partner.dob || ''"/>
                </div>
            </div>
        </xpath>
    </t>

</templates>
